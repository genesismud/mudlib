Synopsys   : (int)VALID_DEF_START_LOCATION(string path)

Defined in : <files.h>

Description: Find out if a certain path is a valid default start location.

Arguments  : string path - the path to the room (without .c) to check.

Returns    : int - if true, it's a valid start location.
