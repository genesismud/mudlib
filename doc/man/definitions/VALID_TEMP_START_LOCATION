Synopsys   : (int)VALID_TEMP_START_LOCATION(string path)

Defined in : <files.h>

Description: Find out if a certain path is a valid temporary start location.

Arguments  : string path - the path to the room (without .c) to check.

Returns    : int - if true, it's a valid start location.
