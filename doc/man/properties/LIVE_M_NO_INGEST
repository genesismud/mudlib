Property   : LIVE_M_NO_INGEST   (and LIVE_I_NO_INGEST)

Defined in : #include <stdproperties.h>

Description: This property is used to indicate that it is not possible for this
             person to ingest something (i.e. eat or whatever verb that has the
             same effect, e.g. nibbling on herbs). The property may return a
             string with a specific failure message.

Returns    : int 1 or string
             if 1, print the default message. If a string, print that.
